{
    // 

    const reverseArray = (arr: any[]) => {
        return arr.slice().reverse();
    }

    // console.log(reverseArray(["apple", "banana", "cherry"]));
    // console.log(reverseArray([1, 2, 3, 4, 5]));
    // console.log(reverseArray(['samir', 42, true, null, { key: 'value:', value: 'test' }]));
    // 
}